#!/bin/bash

if [[ $# -ne 1 ]]; then
    exit 1
fi
prompt=$(echo "$1" | _ansi)
while true; do
    read -p "$prompt [y/N] " -r
    if [[ "${REPLY,,}" =~ ^(y|yes)$ ]]; then
        exit 0
    elif [[ "${REPLY,,}" =~ ^(n|no)$ ]]; then
        exit 1
    fi
done
