#!/bin/bash

# If command-line arguments
if [[ -t 0 ]]; then
    input="$*"

# If standard input
else
    input=$(cat)
fi

# https://www.gnu.org/software/termutils/manual/termutils-2.0/html_chapter/tput_1.html#SEC8
echo "⚠️  $input" | sed "s/\`\([^\`]*\)\`/$(tput smso)\1$(tput rmso)/g"
