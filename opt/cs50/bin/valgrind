#!/bin/bash

. /opt/cs50/lib/cli

# If run on Python program
if [[ "$1" == "python" || "$1" == *.py ]]; then
    _alert "$(_ansi "Afraid \`valgrind\` does not support Python programs!")"
    exit 1
fi

/usr/bin/valgrind "$@"
